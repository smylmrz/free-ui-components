import { UIComponent } from "../../../../../../models/UIComponent";
import { sourceCode } from "./sourceCode";
import { previewCode } from "./previewCode";

export const alertWithDescription = <UIComponent>{
    id: 1,
    name: 'Alert with description',
    groupId: 44,
    previewCode,
    sourceCode
}